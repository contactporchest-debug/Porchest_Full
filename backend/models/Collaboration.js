// Collaboration compatibility model.
// The live app stores collaboration data in the legacy CampaignRequest collection,
// so this file keeps the requested model path available without duplicating state.
module.exports = require('./CampaignRequest');
